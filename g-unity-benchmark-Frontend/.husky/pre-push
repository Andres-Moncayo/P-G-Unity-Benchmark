#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo "ERROR: No se permite hacer push directo a main/master."
  echo "Por favor, crea una rama y haz un pull request."
  exit 1
fi

exit 0
